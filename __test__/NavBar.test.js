import { render, screen } from '@testing-library/react';
import App from '../src/App';
import Navbar from '../src/appComponents/Navbar'

// test to make storage for app
// storage should be undefined, then a function should a system where notes 

test('renders navbar and ');
